# DubFlow sample environment - no secrets here
#
# All variables are placeholders; copy to .env and adjust per machine.

DUBFLOW_ENV=development
DUBFLOW_IPC_PROTOCOL_VERSION=1
DUBFLOW_PYTHON_WORKER_DIR=python-worker
