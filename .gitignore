# Local agent handoff and project-planning documents.
# Keep these files in the working directory, but never commit them.
/AGENTS.md

# Python build/runtime artifacts
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
/build/
/dist/
/dist-installer/
/*.egg-info/

# Local application state (the user can point Workspace elsewhere)
/config/app-settings.json
/workspace/
